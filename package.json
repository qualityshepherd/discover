{
  "name": "discover",
  "version": "1.0.0",
  "description": "Curated RSS discovery. No algorithm. No ads.",
  "type": "module",
  "author": "brine",
  "license": "AGPL",
  "main": "app.js",
  "standard": {
    "env": [
      "browser"
    ]
  },
  "scripts": {
    "deploy": "wrangler deploy",
    "db:migrate:remote": "wrangler d1 migrations apply discover-brine-dev --remote",
    "chalk": "wrangler secret put CHALK_HIT_SECRET",
    "test": "node --test --test-reporter spec test/unit/*.test.js && npx standard .",
    "test:unit": "node --test --test-reporter spec test/unit/*.test.js",
    "test:e2e": "start-server-and-test \"npx wrangler dev --env dev\" http://localhost:8787 \"node --test --test-concurrency=4 --test-reporter spec test/e2e/*.test.js\"",
    "test:debug": "HEADLESS=0 SLOWMO=100 start-server-and-test \"npx wrangler dev --env dev\" http://localhost:8787 \"node --test --test-reporter spec test/e2e/*.test.js\"",
    "kill": "pkill -f chromium",
    "pretty": "npx standard --fix"
  },
  "devDependencies": {
    "puppeteer": "^25.8.0",
    "start-server-and-test": "^2.0.12",
    "wrangler": "^4.123.0"
  }
}
